export interface ReminderEditType {
    action? : string,
    date?: string,
    time?: string,
    description? : string
};
export interface ReminderType {
    action : string,
    date: string,
    time: string,
    description : string
};